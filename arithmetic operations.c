#include<stdio.h>
int main()
{
int a,b;
printf("Enter the two numbers:");
scanf("%d %d",&a,&b);
int c=a+b;
printf("the sum of the two numbers is %d",c);
int d=a*b;
printf("the multiplication of two numbers is %d",d);
int e=a-b;
printf("the substraction of two numbers is %d",e);
float f=a/b;
printf("the division of two numbers is %f",f);	
		return 0;
}
